{
  auto_https off
}

:80 {
    encode gzip

    @api {
        path /api /api/*
    }

    handle @api {
        reverse_proxy backend:8000
    }

    handle /uploads/* {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}
